#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    int n,q;
    cin>>n>>q;
    int a[n];
    int x,y;
    for(x=0;x<n;x++)
    {
        int k;
        cin>>k;
        vector <int> v(k);
        for(y=0;y<k;y++)
        {
            cin>>v[y];
        }
    }
    int i,j;
    cin>>i>>j;

    return 0;
}
